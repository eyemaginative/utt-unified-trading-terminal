[b]UTT — Unified Trading Terminal — Rolling Cumulative Update through September 10, 2026[/b]

This is a cumulative update, not a replacement of prior functionality. The canonical README retains the detailed operator guide, venue coverage, setup instructions, security boundaries, accounting workflows, and historical release notes, with the September 9/10 work added on top.

[b]What UTT currently provides[/b]
[list]
[*]connect and manage venue credentials through an owner-scoped encrypted local API-key vault with metadata-only inventory and read-only migration defaults
[*]inspect balances, available amounts, holds, priced totals, cost basis, average cost, and gain/loss columns
[*]view CEX orderbooks, DEX pseudo-orderbooks, Counterparty dispenser / protocol-order books, synthetic price-context books, and manual route books
[*]submit and track CEX orders, cancel supported venue orders, and monitor venue-native order snapshots
[*]trade through supported live-gated CEX adapters such as Coinbase, Crypto.com, Cexius, Dex-Trade, Gemini, Kraken, Robinhood, and OKX where configured
[*]use Counterparty / UniSat workflows for Bitcoin-metaprotocol assets, collectibles, orderbook context, unsigned compose review, explicit PSBT signing, and separately gated broadcast
[*]use Robinhood Chain for registry-backed EVM balances, exact Token Registry / pair-objective identity, 0x, Uniswap API, and Uniswap v3 RPC read-only indicative quote context, synthetic books, bounded exact-input planning, controlled browser-wallet execution, receipt reconciliation, historical/incremental wallet ingestion, external-swap materialization, All Orders Sync+Load, and deterministic ERC-20 USD balance pricing
[*]submit and track Solana swaps / limit-style flows and confirmed Hydration manual-route swaps
[*]monitor scanners, discovery tools, Ordinals / Counterparty collectibles, wallet activity, market-cap data, volume data, and self-custody balances
[*]filter Market Cap and Volume windows by All / Owned / Unowned and by venue/source such as Coinbase, Crypto.com, Dex-Trade, Gemini, Hydration, Kraken, OKX, Robinhood, self-custody, Solana, and Solana-Jupiter
[*]work with local ledger, deposits, withdrawals, missing-basis lots, transfer links, FIFO lot-impact previews, and tax-related state
[*]preview per-fill basis impact for supported venue fills without mutating FIFO lots
[*]plan cross-chain UTTT movements with bridge-transfer records, canonical supply context, per-chain supply context, and read-only basis previews
[*]manage Token Registry rows, Hydration Route Registry rows, wallet addresses, venue API keys, and local operator preferences
[*]inspect a managed read-only Arbitrage window with best bid/ask, spread, per-venue top-of-book rows, manual refresh, and bounded auto-refresh
[*]integrate Solana and Polkadot / Hydration DEX routing and wallet-based execution alongside traditional exchange adapters
[/list]

[b]Profile / startup / catalog reliability[/b]
[list]
[*]Robinhood Chain discovery and interactive quote limits now persist through the authenticated Profile preference store instead of acting as fixed application ceilings.
[*]The startup Tables tab is database-backed (All Orders, Balances, Local Orders, or Discover) and resolves before the Tables data surface mounts, avoiding an unintended transient balance load.
[*]Registry-discovery writer scope was narrowed around provider work; final POOLS-USDG and VOLT-USDG refresh witnesses completed without the prior SQLite lock failure while background Ledger/FIFO refresh remains enabled.
[/list]

[b]Robinhood Chain[/b]
[list]
[*]Order Book routing now uses bounded provider fallback without mixing bid/ask sides across providers, while structural identity/direction/crossed-market failures remain fail-closed.
[*]Crossed synthetic snapshots use a bounded whole-snapshot resample path; unsafe crossed data is not exposed as a normal Book.
[*]Book → Ticket binding now preserves Robinhood Chain quote-only/synthetic context so Ask → BUY / Exact Spend works without the previous bid-row workaround.
[*]Finite approvals remain finite and separate from swaps. Receipt status can advance through bounded read-only polling, but the watcher cannot prepare, sign, broadcast, or open an automatic second transaction.
[*]Confirmed swaps refresh the relevant balance automatically.
[*]Low-price Robinhood Chain books now use adaptive display/click precision, bounded at 12 decimals, so distinct raw levels are not intentionally collapsed by the old fixed 8/9-decimal frontend limits.
[/list]

[b]Counterparty / XCP[/b]
[list]
[*]XCP direct USD pricing now recovers through its exact CoinGecko identity when the normal market-data response omits that requested ID.
[*]Crossed, stale, rate-limited, one-sided, or excessively wide XCP-BTC books no longer have accounting-price authority. Valid derived fallback requires coherent two-sided evidence.
[*]Live acceptance restored a sane XCP/USD value while preserving the factual crossed Book as market evidence rather than hiding it.
[/list]

[b]Balances / UX[/b]
[list]
[*]Robinhood Chain contract details are now shown in an interactive balance popover with selectable text and Copy contract.
[*]Supported balance mint identities receive the same copy-friendly treatment, including Solana balance surfaces where a mint is present.
[/list]

[b]Validation[/b]
[list]
[*]Canonical backend: 494 passed + 40 subtests, 0 failures.
[*]Frontend production builds are green; existing Vite Solana-import and large-chunk warnings remain visible and non-fatal.
[*]Multiple Robinhood Chain swaps completed successfully during the current accepted workflow.
[/list]

[b]Still intentionally gated[/b]
[list]
[*]No backend signing or transaction broadcast.
[*]No unlimited approvals, automatic retries, or automatic second wallet transactions.
[*]Successful finite-approval auto-advance remains a natural-future live witness; it will not be forced just for validation.
[*]The adaptive low-price Book repair is installed/static-green; the final naturally occurring multi-level display/click witness remains follow-up work after this checkpoint.
[/list]

[b]Detailed operator documentation retained[/b]
[list]
[*]Centralized exchange workflows for Coinbase, Crypto.com Exchange, Cexius, Dex-Trade, Gemini, Kraken, OKX, and Robinhood Crypto where locally configured and live-gated.
[*]Dedicated Robinhood Chain Registry, synthetic Book, browser-wallet execution, receipt reconciliation, wallet-history ingestion, external-swap materialization, pricing, and accounting workflows.
[*]Counterparty / UniSat discovery, dispenser/protocol-order review, unsigned compose, explicit PSBT signing, separately gated broadcast, and accounting previews.
[*]Solana-Jupiter/Raydium routing, Polkadot/Hydration routes, UTTT Spread/Bridge planning, Market Cap/Volume, scanners, Arbitrage, Wallet Addresses, All Venues, ledger, FIFO, and basis workflows.
[*]Complete Quick Start, local environment/secrets model, credential-vault lifecycle, venue-specific setup, live-trading gates, and operational guidance remain in the README.
[/list]

[b]Public repository hygiene[/b]
[list]
[*]Absolute local workstation paths and local user/machine identifiers are omitted from public publication metadata.
[*]Runtime secrets, databases, backups, wallet keys, and private operational material remain outside source control.
[*]Developer regression tests remain local DEV acceptance assets and are not part of the public runtime distribution.
[/list]

The announcement summarizes the cumulative release; the repository README remains the detailed rolling operator reference.
