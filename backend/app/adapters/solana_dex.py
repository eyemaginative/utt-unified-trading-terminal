# backend/app/adapters/solana_dex.py

from __future__ import annotations


class SolanaDexAdapter:
    """
    Placeholder adapter so venue registry can reference solana_dex safely.

    We will flesh this out when we wire:
      - Jupiter quote/build/submit
      - swap status
      - optional normalization to venue_orders
    """

    def __init__(self) -> None:
        pass
